import React from "react"
import { Fade } from "react-reveal";

export const About = () => {
    return (
        <Fade right duration={2000} distane="40px">
            <div>
                <h1>About</h1>
            </div>
        </Fade>
    );
};